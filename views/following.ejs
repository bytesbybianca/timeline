<%- include('partials/header') -%>

<section class="h-100 gradient-custom-2">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-lg-9 col-xl-7">
        <div class="card">
          
            <div class="ms-3" style="margin-top: 130px;">
              <h5 class="text-dark">
                <a href="/profile/<%= profile.id %>">
                  <i class="fas fa-arrow-left pr-1"></i>
                  <% if(profile.name) { %>
                    <%= profile.name %>
                  <% } else { %>
                    <%= profile.userName %>
                  <% } %>
                </a>
              </h5>
            </div>
          
          <div class="p-4 text-black" style="background-color: #f8f9fa;">
            <div class="d-flex justify-content-center text-center py-1">

              <div class="px-3">
                <p class="mb-1 h5"><%= profile.followers.length %></p>
                <p class="small text-muted mb-0">Followers</p>
              </div>
              <div>
                <a href="/profile/<%= profile.id %>/following">
                  <p class="mb-1 h5"><%= profile.following.length %></p>
                  <p class="small text-muted mb-0">Following</p>
                </a>
              </div>
            </div>
          </div>

          <div class="card-body p-4 text-black">

            <div class="mb-5">
              <p class="lead fw-normal mb-1">Following</p>

              <% for(let i = 0; i < following.length; i++) { %>
                <div class="p-4" style="background-color: #f8f9fa;">
                  <div class="row">

                    <div class="mt-3 mb-4 col-2">
                      <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-chat/ava2-bg.webp"
                        class="rounded-circle img-fluid"" />
                    </div>
                    <div class="col-10">
  
                      <h5>
                        <a href="/profile/<%= following[i].id %>"><%= following[i].userName %></a>
                      </h5>
                      <% if(following[i].name) { %>
                        <h6><%= following[i].name %></h6>
                      <% } %>
                      
                      <p>
                      <%# if this person also follows you %>
                      <% if(following[i].following.includes(user.id)) { %>
                          This person follows you.
                      <%# if this person does not follow you %>
                      <% } else { %>
                        This person does not follow you.
                      <% } %>
                      </p>
                    </div>
                  </div>
                </div>
              <% } %>

            </div>
            
        </div>
      </div>
      </div>
    </div>
  </div>
</section>

<%- include('partials/footer') -%>